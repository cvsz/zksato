# Skill: observability

Instrument structured logs, metrics, and traces with correlation IDs from signal through fill. Redact secrets.

Minimum signals: feed age, broker latency/error, order state counts, reconciliation lag, risk rejects, exposure, P&L/drawdown, bot state, DB/Redis health, auth failures, kill switch.

Every alert must have an owner, threshold rationale, and runbook action.

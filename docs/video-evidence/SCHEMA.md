# Video evidence manifest schema

A manifest records source identity, media metadata, directly observed facts and explicitly separated inferences. Source media and extracted frames are not committed by default.

Required review rule: an inferred trading rule must never be presented as something directly visible in a recording, and selected profitable frames must never be treated as profitability evidence.
